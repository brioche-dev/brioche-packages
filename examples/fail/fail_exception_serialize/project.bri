import * as std from "std";

export const project = {
  dependencies: {
    std: "*",
  },
};

interface TestType {
  foo: string;
  bar: string;
}

export default () => {
  return {
    briocheSerialize: () => {
      throw new Error("briocheSerialize exception");
    }
  }
}
