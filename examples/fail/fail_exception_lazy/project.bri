import * as brioche from "std";

export const project = {
  dependencies: {
    std: "*",
  },
};

interface TestType {
  foo: string;
  bar: string;
}

export const value = brioche.memo((): brioche.Lazy => {
  throw new Error("lazy memo exception");
})

export default () => {
  const result = value();

  return result;
}
